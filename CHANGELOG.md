# Changelog

## v0.3.2（2026-08-30）

### 修复（移动端）
- **面包屑被挤压**：面包屑与工具栏按钮同行时被压缩成半截片段。改为独占一行（`flex-basis:100%`），完整横向滚动显示

## v0.3.1（2026-08-30）

### 修复（移动端）
- **面包屑竖排问题**：窄屏下面包屑路径被折成多行。改为单行横向滚动（隐藏滚动条），目录切换后自动滚到最右端显示当前目录段

## v0.3.0（2026-08-30）

### 新增
- **图片预览**：点击常见图片文件（png / jpg / jpeg / gif / webp / bmp / svg / ico / avif）直接在预览区显示，不再提示「二进制文件」
  - 服务端新增 `/disk-browser/api/image` 路由：扩展名白名单校验后按正确 content-type 直出文件字节
  - 客户端对图片扩展名跳过文本读取，直接渲染自适应居中的 `<img>`
  - 预览页的「下载」按钮对图片同样可用

### 优化（移动端）
- **路径栏自动滚动**：目录切换后路径输入框自动滚动到末尾，窄屏上不再只显示长路径的开头部分

### 升级说明
- 无配置项变化，直接替换文件 / `git pull` 后重启 dsh 即可
- 图片预览依赖新增的 `/disk-browser/api/image` 路由：子路径反代部署（如 nginx `location /disk-browser/`）无需改动，原有透传规则已覆盖

## v0.2.0

- 侧边栏磁盘文件夹浏览器：目录列表（含隐藏文件开关）、文本预览（256 KB 截断）、下载、上传（含覆盖确认）、面包屑导航
